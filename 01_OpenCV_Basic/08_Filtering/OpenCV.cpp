#include "OpenCV.h"

using namespace std;
using namespace cv;

int main(void) {
    string openPath = path + image;
	Mat LennaGray = imageRead(openPath, IMREAD_GRAYSCALE);

	destroyAllWindows();

	return 0;
}
